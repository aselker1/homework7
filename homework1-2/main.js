// const task = (number) => {
// if (number === 1) {
//     return 1;
// } 

// const next = task (number -1);
// return number + next;
// };

// const result = task(5);
// console.log(result); 


// const task2 =(a,b) => {

// }

const task = (number) => {
     if (number === 1) {
         return 1;
     } 
    
     const next = task (a,b,c);
     return a+b+c;
     };
    
     const result = task(1,7,9);
     console.log(result); 
    
    